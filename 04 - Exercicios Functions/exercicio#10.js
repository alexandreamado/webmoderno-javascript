// Crie uma função que verifica se um número inteiro passado como parêmetro é divisível por 3 e retorne true
// ou false. 

function retorno(inteiro) {
    if(inteiro % 3 == 0) {
        console.log(true)
    }else {
        console.log(false)
    }
} 

retorno(3)
retorno(2)
retorno(150)